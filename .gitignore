# Dependencies
node_modules/

# Build outputs
# dist/ 在部署环境（内网）从源码 npm run build 生成，不进 git
dist-ssr/

# E2E test reports (generated artifacts)
tests/e2e/reports/
target/
frontend/dist/
backend/agent-core/dist/
frontend/dist.zip
frontend/dist 2.zip

# IDE & OS
.DS_Store
# Accidental nested checkout (do not commit)
backend/agent-core/go-cursor-help/
.cursor/
.vscode/
.idea/
*.log

# Env & secrets
.env
.env.local
*.local

# Local dev config (per-developer MySQL password etc.)
# Use the .example template:
#   cp backend/skill-gateway/src/main/resources/application-prod.example.properties \
#      backend/skill-gateway/src/main/resources/application.properties
backend/skill-gateway/src/main/resources/application.properties
backend/skill-gateway/src/test/resources/application.properties
backend/skill-gateway/src/main/resources/application-local.properties

# Database & runtime data
backend/skill-gateway/data/
backend/skill-gateway/ftp_root/
# Maven 本地 repo（100MB+，离线 build 用，不进 git；拷贝到内网用 Desktop 上的 m2-offline-bundle.tar.*）
backend/skill-gateway/.m2/
backend/skill-gateway/.m2-test
backend/skill-gateway/skills-export.csv
backend/skill-gateway/seed-snapshot.mv.db
backend/agent-core/memories.db

# Backups
frontend-react-backup/
0427/


# skills
skills/
# .trae/skills/ 是项目 Trae AI 技能，需要提交
!.trae/skills/

# Local dev tools (per-developer, not committed)
# e.g. .dev-ftp-test/ — 本地测试用 FTP + 启动/停止/状态脚本
.dev-*/

# Maven
backend/skill-gateway/apache-maven-*/

# One-click scripts（Windows 一键启停，本地用，不进 git）
start-all.bat
start-all.ps1
stop-all.bat
stop-all.ps1
start-services.bat
stop-services.bat
start-all.bat
start-all.ps1
stop-all.bat
stop-all.ps1

# Logs (runtime application logs)
logs/

# Docs (root-level design / hand-off docs, not in git)
docs/

# llm-context-window-summarization change 跳过的测试代码（仅本地/CI 验证，不进 git）
backend/agent-core/test/gateway-compact-client.test.cjs
backend/agent-core/scripts/e2e-compact.sh
backend/skill-gateway/src/test/java/com/lobsterai/skillgateway/service/ConversationCompactServiceTest.java
backend/skill-gateway/src/test/java/com/lobsterai/skillgateway/controller/ConversationControllerCompactTest.java

# 所有 test 文件夹整体不进 git（Java src/test/ + agent-core test/ + 其他子项目 test/）
**/test/

# 所有 dist.zip 打包文件不进 git
**/dist.zip
