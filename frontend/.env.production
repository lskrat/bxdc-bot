VITE_API_URL=/
VITE_AGENT_URL=/

# open spec: add-slash-skill-invocation — enable slash / hash skill picker in MessageInput
# true|1|yes|on → 输入框以 / 或 # 开头时显示技能选择器（仅 frontend 侧）
# 配合 backend 的 AGENT_SLASH_SKILL_INVOCATION=true 才是完整端到端
VITE_SLASH_SKILL_INVOCATION=true