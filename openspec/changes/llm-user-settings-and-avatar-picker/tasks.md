## 1. 数据模型与网关 API

- [x] 1.1 在 skill-gateway 为用户 LLM 设置增加持久化（字段或关联表：`apiBase`、`modelName`、`apiKey`；首版明文存储）
- [x] 1.2 实现已认证用户的 GET/PUT（或 PATCH）LLM 设置接口；GET 响应掩码或不返回明文 Key
- [x] 1.3 为任务或内部调用提供按 `userId` 解析合并后 LLM 配置的能力（供 agent-core 消费或注入）

## 2. agent-core 集成

- [x] 2.1 在 `runTask`（及需使用用户 LLM 的附属路径）在存在 `userId` 时先加载用户持久化配置，再与 `OPENAI_*` 环境变量合并（用户非空字段优先），保证保存后的配置实际生效
- [x] 2.2 将合并结果传入 `AgentFactory.createAgent`；覆盖记忆判断等仍读 env 的路径时与主对话策略一致或文档说明限制
- [x] 2.3 头像生成接口使用与主对话一致的 merge 后配置；merge 后无 API Key 时接口返回错误或前端禁用「自动生成」

## 3. 前端

- [x] 3.1 新增「大模型设置」页面或抽屉：base URL、模型名、API Key、保存与已配置状态展示
- [x] 3.2 注册/用户资料：集成 emoji 选择器为默认路径；「自动生成」在无可用 API Key 时禁用，否则显式调用后端
- [x] 3.3 对接网关 LLM 设置 API 与类型；确保不在控制台或状态中持久化明文 Key

## 4. 文档与验证

- [x] 4.1 更新部署/单主机文档：说明用户设置覆盖 env 默认及安全注意点
- [x] 4.2 联调：未配置用户走 env、已配置用户走自定义；头像手动与自动生成路径
