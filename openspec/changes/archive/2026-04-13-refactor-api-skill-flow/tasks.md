## 1. API Skill 配置结构重构

- [x] 1.1 梳理现有 API Skill `configuration` 协议，定义新的路由摘要、接口说明和参数格式契约字段结构
- [ ] 1.2 更新后端 API Skill 配置解析与序列化逻辑，兼容历史配置并为新字段提供统一访问方式
- [ ] 1.3 更新前端 API Skill 结构化表单，支持编辑接口说明与参数格式契约，并补充保存前校验

## 2. Agent 渐进披露与运行时校验

- [ ] 2.1 调整 API Skill 动态注册逻辑，使路由阶段仅暴露简要 `description`
- [ ] 2.2 在 API Skill 实际调用链路中注入详细接口说明，确保 Agent 在生成参数前可读取完整字段语义
- [ ] 2.3 在 API Skill 请求发送前实现严格参数格式校验，并返回结构化错误而不是直接请求外部 API
- [ ] 2.4 为默认值补齐、缺字段、类型错误和枚举值错误补充运行时测试

## 3. API Skill 生成工具升级

- [ ] 3.1 更新 `skill_generator` 的输入要求与缺失字段提示，使其能够采集接口说明和参数格式契约
- [ ] 3.2 调整生成后的 API Skill payload，确保 `description`、接口说明和参数契约分别落到新结构
- [ ] 3.3 更新生成后验证逻辑与测试用例，确保生成出的 API Skill 可被新运行时直接消费

## 4. 验证与回归

- [x] 4.1 为前端结构化编辑器补充解析、回填和序列化测试
- [x] 4.2 为后端 Skill discovery、调用执行和生成链路补充回归测试
- [x] 4.3 执行相关构建与测试命令，确认 API Skill 编辑、生成和调用链路均通过验证
