## 1. 字段名称映射更新

- [x] 1.1 在前端或 API 数据转换层中，将 Skill 的 `Name` 字段中文展示名称修改为“名称”
- [x] 1.2 在前端或 API 数据转换层中，将 Skill 的 `Description` 字段中文展示名称修改为“技能介绍”
- [x] 1.3 验证所有展示 Skill 列表的页面，确保字段名称更新生效

## 2. 延迟加载逻辑实现

- [x] 2.1 修改 `loadGatewayExtendedTools`（或相关获取工具列表的函数），使其在初始加载时仅返回 `Name` 和 `Description` 字段
- [x] 2.2 增加或修改确认调用 Skill 时的逻辑，根据 Skill ID 动态加载该 Skill 的全量参数
- [x] 2.3 在前端/调用端适配延迟加载逻辑，确保在确认调用前不会因缺少参数而报错
- [x] 2.4 测试完整的 Skill 发现和调用流程，确保延迟加载不会影响 Skill 的正常执行